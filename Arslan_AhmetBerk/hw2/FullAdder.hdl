CHIP FullAdder {
    IN a, b, cin;  // Inputs: a, b, carry-in
    OUT sum, cout; // Outputs: sum, carry-out

    PARTS:
    // Half Adders
    HalfAdder(a=a, b=b, sum=s1, carry=c1);
    HalfAdder(a=s1, b=cin, sum=sum, carry=c2);
    // OR gate for carry-out
    Or(a=c1, b=c2, out=cout);
}
